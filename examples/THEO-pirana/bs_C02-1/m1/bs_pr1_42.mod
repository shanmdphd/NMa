$PROBLEM    THEOPHYLLINE ORAL
$INPUT      ID AMT=DROP TIME DV BWT
$DATA      bs_pr1_42.dta
$PRED
  DOSE = 320
  V    = THETA(1) * EXP(ETA(1))
  K    = THETA(2) * EXP(ETA(2))
  KA   = THETA(3) * EXP(ETA(3))
  F    = DOSE / V * KA / (KA - K) * (EXP(-K*TIME) - EXP(-KA*TIME))
  IPRED= F
  Y    = F + EPS(1)

  IF (ICALL.EQ.3) THEN
    WRITE (53,*) OMEGA(BLOCK)
    WRITE (54,*) SIGMA(BLOCK)
  ENDIF

$THETA  (0,32.1362,500)
 (0,0.087707,10)
 (0,1.58774,50)
$OMEGA  BLOCK(3)
 0.0189943
 0.0135975 0.0186052
 0.0564696 -0.00780131 0.43007
$SIGMA  0.477171
$ESTIMATION MAX=9999 METHOD=COND

